.env
.claude/
.pi/
.polly/
.worktrees/

# Jekyll build artifacts (local preview only)
_site/
.jekyll-cache/
.jekyll-metadata
Gemfile.lock
vendor/

# Local-only preview shim: the real /blog/ pages are generated by Jekyll on
# GitHub Pages from blog.html + _posts/ + _layouts/. Never commit /blog/.
/blog/
